services:
  ldavincif-portal:
    build:
      context: .
      dockerfile: Dockerfile
    image: ldavincif/ldavincif-portal
    container_name: ldavincif-portal
    restart: unless-stopped
    ports:
      - "8080:8080"
    security_opt:
      - no-new-privileges:true
